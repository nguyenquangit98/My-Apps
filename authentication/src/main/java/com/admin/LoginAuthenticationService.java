package com.admin;

import com.admin.entities.GlobalServiceError;
import io.vavr.control.Validation;

public record LoginAuthenticationService(LoginAuthenticationDao loginAuthenticationDao) {
    public Validation<GlobalServiceError, >
}
